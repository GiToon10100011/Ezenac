import React from "react";

let initialState = {
  data: [],
};

const pokeReducer = (state = initialState, action: object) => {
  // const { type, payload } = action;
  // switch (type) {
  //   case "GET_DATA_SUCCESS":
  //     return { ...state, data: payload.data };
  // }
};

export default pokeReducer;
